version: "3.9"

services:
  postgres:
    image: postgres:15
    container_name: galit_postgres
    restart: always
    environment:
      POSTGRES_USER: galit
      POSTGRES_PASSWORD: galit
      POSTGRES_DB: galit_crm
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: galit_redis
    restart: always
    ports:
      - "6379:6379"

volumes:
  postgres_data: