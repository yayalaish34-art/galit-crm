-- "ליד חדש נכנס" שהועבר לעובד אחר חייב להופיע בראש רשימת המשימות שלו.
-- הסימון היחיד שקיים היום ("אין customerId") נמחק בהעברה עצמה, כי ההעברה שומרת
-- את כרטיס הלקוח לפני שהיא מחליפה בעלים. עמודה זו שומרת את רגע ההעברה.
ALTER TABLE "Task" ADD COLUMN IF NOT EXISTS "transferredAt" TIMESTAMP(3);
